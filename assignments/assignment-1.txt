1. create a .NET application for calculation

	a. create a library with a class that must have another 4 instance methods:
		add, subtract, multiply and divide => these methods can add, multiply, subtract or divide any two integer type values and return the resultant value
	arguments: all accepts two argument of type int
	return type: return type of all of them is int

	b. create a console application in he same solution and call these methods from main method based on a choice from user
	the application should continue running untill and unless and user wants to terminate it by entering some choice
	
	Note: accept the choice as well two values from console and them pass to the methods as parameters. also print a menu for user, so that user can make a choice.